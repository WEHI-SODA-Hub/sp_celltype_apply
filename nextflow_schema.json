{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/MIBI-apply-model-nf/master/nextflow_schema.json",
    "title": "MIBI-apply-model-nf pipeline parameters",
    "description": "Pipeline to apply a trained XGBoost model to unlabelled preprocessed QuPath data.",
    "type": "object",
    "$defs": {
        "input_output_options": {
            "title": "Input/output options",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "description": "Define where the pipeline should find input data and save output data.",
            "properties": {
                "input": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "pattern": "^\\S+\\.(csv|parquet)$",
                    "description": "Preprocessed input data file from QuPath (.csv or .parquet). E.g., /home/users/allstaff/<username>/batch1-apply_preprocessed_input_data.csv",
                    "fa_icon": "fas fa-file-csv"
                },
                "run_name": {
                    "type": "string",
                    "description": "Run name used to label output files. E.g., batch1-apply.",
                    "fa_icon": "fas fa-align-justify"
                },
                "model": {
                    "type": "string",
                    "description": "Final model saved from training. E.g., /vast/scratch/users/$USER/mibi-batch1-workspace/batch1-training-logp1-SMOTE-1/bayes_cv_model.sav.\nNOTE: if using the GPU version, make sure to use the bayes_cv_model.sav file, not the final_model.sav file!",
                    "fa_icon": "fas fa-file-alt",
                    "format": "file-path",
                    "pattern": "^\\S+\\.sav$"
                },
                "preprocess_scheme": {
                    "type": "string",
                    "description": "The scheme used to preprocess the model before application. E.g., logp1",
                    "fa_icon": "fas fa-align-justify",
                    "enum": [
                        "null",
                        "logp1",
                        "poly"
                    ]
                },
                "decoder_file": {
                    "type": "string",
                    "description": "JSON file containing the decoder for the predicted cell types. E.g., /home/users/allstaff/<username>/batch1-apply_decoder.json",
                    "fa_icon": "fas fa-file-alt",
                    "format": "file-path",
                    "mimetype": "application/json",
                    "pattern": "^\\S+\\.json$"
                },
                "images_list": {
                    "type": "string",
                    "description": "Path to images file produced during preprocessing (.csv or .parquet). E.g., /home/users/allstaff/<username>/batch1-apply_images.csv",
                    "fa_icon": "fas fa-file-csv",
                    "format": "file-path",
                    "pattern": "^\\S+\\.(csv|parquet)$"
                },
                "validation_file": {
                    "type": "string",
                    "description": "Path to preprocessed input data used to train the XGBoost model (.csv or .parquet). This is used to validate the input data. E.g., /home/users/allstaff/<username>/training_preprocessed_input_data.csv",
                    "fa_icon": "fas fa-file-csv",
                    "format": "file-path",
                    "pattern": "^\\S+\\.(csv|parquet)$"
                },
                "output_path": {
                    "type": "string",
                    "description": "Path to directory to store output files. E.g., /home/users/allstaff/<username>/batch1-apply-results",
                    "fa_icon": "fas fa-folder",
                    "format": "directory-path"
                },
                "options_file": {
                    "type": "string",
                    "default": "${projectDir}/assets/NO_FILE",
                    "fa_icon": "fas fa-file-alt",
                    "description": "TOML file containing preprocessing scheme and model classifier options. Only needed if --preprocess_scheme=poly. E.g., /home/users/allstaff/<username>/options.toml",
                    "format": "file-path"
                },
                "threshold": {
                    "type": "string",
                    "description": "Not sure what this does yet",
                    "fa_icon": "fas fa-align-justify"
                }
            },
            "required": [
                "input",
                "run_name",
                "model",
                "preprocess_scheme",
                "images_list",
                "output_path",
                "decoder_file"
            ]
        },
        "generic_options": {
            "title": "Generic options",
            "type": "object",
            "fa_icon": "fas fa-file-import",
            "description": "Less common options for the pipeline, typically set in a config file.",
            "help_text": "These options are common to all nf-core pipelines and allow you to customise some of the core preferences for how the pipeline runs.\n\nTypically these options would be set in a Nextflow config file loaded for all pipeline runs, such as `~/.nextflow/config`.",
            "properties": {
                "help": {
                    "type": "boolean",
                    "description": "Display help text.",
                    "fa_icon": "fas fa-question-circle",
                    "hidden": true
                },
                "version": {
                    "type": "boolean",
                    "description": "Display version and exit.",
                    "fa_icon": "fas fa-question-circle",
                    "hidden": true
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/$defs/input_output_options"
        },
        {
            "$ref": "#/$defs/generic_options"
        }
    ],
    "properties": {
        "memory": {
            "type": "string",
            "default": "64 GB",
            "hidden": true,
            "description": "How much memory needed to apply the model."
        }
    }
}